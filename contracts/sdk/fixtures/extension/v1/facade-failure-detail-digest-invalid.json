{
  "name": "facade-failure-detail-digest-invalid",
  "kind": "invalid",
  "target": "FacadeFailureDetail",
  "description": "A claimed digest inside failure details must still be a sha256 digest.",
  "payload": {
    "operation": "echo_agent::eval::runner::EvalRunner",
    "signature_digest": "not-a-digest"
  },
  "expect_error": "invalid_value"
}